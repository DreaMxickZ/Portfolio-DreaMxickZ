# คู่มือการใช้งาน Portfolio Website

## 📋 สารบัญ

1. [การติดตั้งครั้งแรก](#การติดตั้งครั้งแรก)
2. [การแก้ไขข้อมูลส่วนตัว](#การแก้ไขข้อมูลส่วนตัว)
3. [การเพิ่มผลงานใหม่](#การเพิ่มผลงานใหม่)
4. [การเพิ่มรูปภาพ](#การเพิ่มรูปภาพ)
5. [การเพิ่มวิดีโอ YouTube](#การเพิ่มวิดีโอ-youtube)
6. [การเปิด/ปิด Demo](#การเปิดปิด-demo)
7. [การแก้ไขสี](#การแก้ไขสี)

---

## การติดตั้งครั้งแรก

### 1. ติดตั้ง Node.js
ดาวน์โหลดและติดตั้ง Node.js จาก [nodejs.org](https://nodejs.org)

### 2. เปิด Terminal/Command Prompt
- **Windows**: กด `Win + R` พิมพ์ `cmd`
- **Mac**: กด `Cmd + Space` พิมพ์ `terminal`

### 3. ติดตั้ง Dependencies
```bash
cd path/to/portfolio
npm install
```

### 4. รันโปรเจ็กต์
```bash
npm run dev
```

### 5. เปิดเบราว์เซอร์
ไปที่ `http://localhost:3000`

---

## การแก้ไขข้อมูลส่วนตัว

### แก้ไขหน้า About

เปิดไฟล์: `app/about/page.tsx`

#### เปลี่ยนชื่อ
```typescript
<h2 className="text-3xl font-bold mb-4">[ชื่อของคุณ]</h2>
```
เปลี่ยนเป็น:
```typescript
<h2 className="text-3xl font-bold mb-4">สมชาย ใจดี</h2>
```

#### เปลี่ยน Email
```typescript
<a href="mailto:your.email@example.com">
```
เปลี่ยนเป็น:
```typescript
<a href="mailto:somchai@example.com">
```

#### เปลี่ยน GitHub Username
```typescript
<a href="https://github.com/yourusername">
```
เปลี่ยนเป็น:
```typescript
<a href="https://github.com/somchai123">
```

#### แก้ไขการศึกษา
หาส่วนนี้:
```typescript
<h3 className="text-xl font-semibold">ปริญญาตรี วิศวกรรมคอมพิวเตอร์</h3>
<p className="text-gray-600">มหาวิทยาลัย [ชื่อมหาวิทยาลัย]</p>
<p className="text-gray-500 text-sm">2020 - 2024</p>
```

เปลี่ยนเป็นข้อมูลของคุณ

#### แก้ไขทักษะ
หาส่วน Skills และแก้ไขเป็นทักษะที่คุณมี

---

## การเพิ่มผลงานใหม่

### เปิดไฟล์ข้อมูลผลงาน

ไฟล์: `data/projects.ts`

### เพิ่มผลงาน Hardware

```typescript
{
  id: 'my-arduino-project',           // ต้องไม่ซ้ำกับผลงานอื่น
  title: 'Arduino Smart Light',       // ชื่อโปรเจ็กต์
  category: 'hardware',                // ประเภท: hardware
  description: 'ไฟอัจฉริยะควบคุมด้วยมือถือ',  // คำอธิบายสั้น
  details: 'สร้างระบบควบคุมไฟด้วย Arduino และ ESP8266...', // รายละเอียดยาว
  images: [                            // รูปภาพ (ใส่ได้หลายรูป)
    'https://example.com/image1.jpg',
    'https://example.com/image2.jpg',
  ],
  youtubeId: 'ABC123XYZ',             // YouTube Video ID (ถ้ามี)
  demoUrl: 'https://demo.com',        // URL Demo (ถ้ามี)
  githubUrl: 'https://github.com/user/project', // GitHub (ถ้ามี)
  tags: ['Arduino', 'IoT', 'WiFi'],   // Tags
  date: '2024-01'                      // วันที่ (YYYY-MM)
}
```

### เพิ่มผลงาน Software

```typescript
{
  id: 'my-website',
  title: 'E-Commerce Website',
  category: 'software',     // ประเภท: software
  // ... ส่วนที่เหลือเหมือนกัน
}
```

### เพิ่มผลงาน AI

```typescript
{
  id: 'face-detection',
  title: 'Face Detection App',
  category: 'ai',           // ประเภท: ai
  // ... ส่วนที่เหลือเหมือนกัน
}
```

**อย่าลืม:** ใส่เครื่องหมาย `,` (comma) ต่อท้ายแต่ละโปรเจ็กต์

---

## การเพิ่มรูปภาพ

### วิธีที่ 1: ใช้รูปภาพจาก URL

```typescript
images: [
  'https://images.unsplash.com/photo-123456',
  'https://example.com/my-image.jpg',
]
```

### วิธีที่ 2: ใช้รูปภาพในโปรเจ็กต์

1. สร้างโฟลเดอร์: `public/images/projects/`
2. วางไฟล์รูปภาพ เช่น `robot-arm-1.jpg`
3. ใช้งาน:

```typescript
images: [
  '/images/projects/robot-arm-1.jpg',
  '/images/projects/robot-arm-2.jpg',
]
```

### คำแนะนำรูปภาพ
- **ขนาดที่แนะนำ**: 1200 x 800 pixels
- **ขนาดไฟล์**: ไม่เกิน 500 KB ต่อรูป
- **ชนิดไฟล์**: .jpg, .png, .webp

---

## การเพิ่มวิดีโอ YouTube

### หา YouTube Video ID

1. เปิดวิดีโอใน YouTube
2. ดู URL ในเบราว์เซอร์

**ตัวอย่าง:**
```
https://www.youtube.com/watch?v=dQw4w9WgXcQ
                                 ^^^^^^^^^^^^
                                 นี่คือ Video ID
```

3. Copy Video ID
4. เพิ่มในโปรเจ็กต์:

```typescript
youtubeId: 'dQw4w9WgXcQ',
```

### ไม่มีวิดีโอ YouTube?
ไม่ต้องใส่ `youtubeId` หรือใส่เป็น:
```typescript
youtubeId: undefined,
```

---

## การเปิด/ปิด Demo

### มี Demo ที่ให้ดูได้
```typescript
demoUrl: 'https://my-project-demo.vercel.app',
```

### ไม่มี Demo
ไม่ต้องใส่ `demoUrl` หรือใส่เป็น:
```typescript
demoUrl: undefined,
```

ระบบจะแสดงปุ่ม "ไม่มี Demo" สีเทาอัตโนมัติ

---

## การแก้ไขสี

### เปลี่ยนสีหลักของเว็บไซต์

เปิดไฟล์: `app/page.tsx` (หน้าแรก)

#### เปลี่ยนสี Hero Section
หาส่วนนี้:
```typescript
<section className="bg-gradient-to-br from-blue-600 to-purple-700">
```

เปลี่ยนสี:
- `blue-600` → สีอื่นๆ เช่น `green-600`, `red-600`
- `purple-700` → สีอื่นๆ

#### เปลี่ยนสีปุ่ม
```typescript
<Link className="bg-blue-600 text-white ...">
```

เปลี่ยน `blue-600` เป็นสีที่ต้องการ

### สีที่ใช้ได้ใน Tailwind CSS

- `red-600` - สีแดง
- `orange-600` - สีส้ม
- `yellow-600` - สีเหลือง
- `green-600` - สีเขียว
- `blue-600` - สีน้ำเงิน
- `indigo-600` - สีคราม
- `purple-600` - สีม่วง
- `pink-600` - สีชมพู

เปลี่ยนตัวเลข `600` เป็น `400-900` เพื่อปรับความเข้มของสี

---

## Tips & Tricks

### 1. บันทึกไฟล์ทุกครั้งที่แก้ไข
หลังแก้ไขไฟล์ กด `Ctrl + S` (Windows) หรือ `Cmd + S` (Mac)

### 2. รีเฟรชเบราว์เซอร์
ถ้าไม่เห็นการเปลี่ยนแปลง กด `F5` หรือ `Ctrl + R`

### 3. ตรวจสอบ Console
เปิด Developer Tools (`F12`) เพื่อดู Error ถ้ามี

### 4. Restart Server
ถ้าแก้ไขแล้วไม่ทำงาน:
1. ปิด Terminal (กด `Ctrl + C`)
2. รันใหม่: `npm run dev`

---

## เกิดปัญหา?

### ปัญหาที่พบบ่อย

#### 1. เว็บไม่แสดงผล
- ตรวจสอบว่าใส่ comma `,` ครบทุกที่
- ตรวจสอบว่า quotes `""` ปิดครบถ้วน

#### 2. รูปภาพไม่แสดง
- ตรวจสอบ URL ว่าถูกต้อง
- ตรวจสอบว่าไฟล์รูปอยู่ในโฟลเดอร์ `public/`

#### 3. YouTube ไม่แสดง
- ตรวจสอบว่า Video ID ถูกต้อง
- ตรวจสอบว่าวิดีโอเป็น Public

#### 4. Error ใน Terminal
- อ่าน Error message
- Google ข้อความ Error
- ตรวจสอบ syntax (`,`, `""`, `{}`, `[]`)

---

## ติดต่อสอบถาม

หากมีคำถามหรือต้องการความช่วยเหลือ:
- เปิด Issue ใน GitHub
- ดูเอกสาร Next.js: [nextjs.org/docs](https://nextjs.org/docs)
- ดูเอกสาร Tailwind CSS: [tailwindcss.com/docs](https://tailwindcss.com/docs)

---

**สร้างโดย:** Next.js + React + TypeScript  
**เวอร์ชัน:** 1.0.0  
**อัปเดตล่าสุด:** มกราคม 2026
